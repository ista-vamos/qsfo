[project]
name = "qsfo"
version = "0.1.0"
description = "Monitoring quantitative signal first-order logic"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lark>=1.3.1",
    "pplpy>=0.8.10",
    "sympy>=1.14.0",
]
