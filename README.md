# qsfo
Quantitative Signal First-Order Logic

## Setup

```sh
# setup python virtual environment
python3 -mvenv .venv/
source .venv/bin/activate

pip install -r requirements.txt
```


## Usage

In every terminal from where you run the project,
you must first call

```sh
source .venv/bin/activate
```


